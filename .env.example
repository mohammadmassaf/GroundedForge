GROQ_API_KEY=your_key_here

# Optional: a free Hugging Face token (huggingface.co/settings/tokens).
# Not required, but anonymous model downloads for the cross-encoder
# re-ranker (M7) are throttled — a token makes them fast and reliable.
HF_TOKEN=your_hf_token_here
